apiVersion: v1
kind: Service
metadata:
  name: {{ include "crownx-process-images-app.fullname" . }}
  labels:
    {{- include "crownx-process-images-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crownx-process-images-app.selectorLabels" . | nindent 4 }}
